import {styled} from "styled-components/"

export const StyledList = styled.ul`
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    margin: 0;
    padding-left: 0;
    gap: 15px;
    list-style: none;
`